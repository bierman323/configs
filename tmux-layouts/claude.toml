description = "Claude Code dev session"

[tab]
title = "claude-dev"
color = [80, 180, 80]

# Each [[windows]] entry becomes one Claude pane in the overview window.
# The shell pane is added automatically — do not define it here.

[[windows]]
name = "claude"
command = "claude --resume || claude"
