CORP_CA_BUNDLE="${HOME}/awn-corp-ca-bundle.pem"
export SSL_CERT_FILE="${CORP_CA_BUNDLE}" # openssl / pip / golang / rust / php
export REQUESTS_CA_BUNDLE="${CORP_CA_BUNDLE}" # requests
export AWS_CA_BUNDLE="${CORP_CA_BUNDLE}" # boto3
export CURL_CA_BUNDLE="${CORP_CA_BUNDLE}" # curl
export HTTPLIB2_CA_CERTS="${CORP_CA_BUNDLE}" # httplib2
export NODE_EXTRA_CA_CERTS="${CORP_CA_BUNDLE}" # nodejs

# Setup some aliases
alias cldt="cldt.sh"
alias cldr="claude --continue"
